<%= simple_form_for(@post, url: path) do |f| %>
  <%= f.input :title, label: "Titre" %>
  <%= f.input :content, as: :text %>
  <%= f.input :photo, as: :file %>
  <%= f.button :submit, value: 'Publier cet article'%>
<% end %>

<%= link_to "Retour à tous les articles", posts_path %>

